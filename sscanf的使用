#include <stdio.h>

int main()
{
    char buffer[] = "10 3.14";
    int n;
    float f;

    sscanf(buffer, "%d %f", &n, &f);
    printf("int: %d, float: %.2f\n", n, f); // 输出：Integer: 10, Float: 3.14

    return 0;
}
